query confirmPhoneNumber($command: InputConfirmPhoneNumberType) {
    confirmPhoneNumber(command: $command) {
        succeeded
        errors {
            code
            parameter
            description
        }
    }
}